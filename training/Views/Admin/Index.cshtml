@model IEnumerable<DatabaseFirst.word> 
@{
//ViewBag.Title = "Данные пользователей";
}

<div class="row">
    <div class="col-md-10">
      @*  @Html.ActionLink("<< Назад", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })*@
    </div>
</div>

<div class="panel panel-primary">
    <div class="panel-heading">Упражнения</div>
        <table class="table table-striped">
            @if (Model.Count() == 0)
            {
                <tr>
                    <td colspan="4" class="text-center">Упражнений нет</td>
                </tr>
            }
            else
            {
                foreach (var item in Model)
                {
                <tr>
                    <td>@item.word1</td>
                    @*<td>

            @Html.ActionLink("Назначить учеников", "Student", new { id = item.Id },
                new { @class = "btn btn-primary btn-xs", style = "float: left; margin-right: 10px" })
        </td>*@
                </tr>
                }
            }
        </table>
</div>
